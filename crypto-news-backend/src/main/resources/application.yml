server:
  error:
    include-message: always
    include-binding-errors: always

spring:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: true
  datasource:
    username: root
    password: root
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/crypto_news_db